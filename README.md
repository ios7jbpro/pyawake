# pyawake
You know how PowerToys has Awake? This is the same thing, but just the Awake feature only, light, written in Python, shipped as an executable binary.

# Building this project
`pip install pyinstaller
pyinstaller --onefile main.py`

# Creating an installer
Use Inno Setup with the iss file provided in the repository.
